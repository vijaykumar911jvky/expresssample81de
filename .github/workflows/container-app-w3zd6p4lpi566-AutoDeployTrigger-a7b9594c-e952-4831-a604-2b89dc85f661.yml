name: Trigger auto deployment for container-app-w3zd6p4lpi566

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-w3zd6p4lpi566-AutoDeployTrigger-a7b9594c-e952-4831-a604-2b89dc85f661.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPW3ZD6P4LPI566_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPW3ZD6P4LPI566_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPW3ZD6P4LPI566_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPW3ZD6P4LPI566_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPW3ZD6P4LPI566_REGISTRY_PASSWORD }}
          containerAppName: container-app-w3zd6p4lpi566
          resourceGroup: expresssample81de8b4d
          imageToBuild: default/container-app-w3zd6p4lpi566:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


